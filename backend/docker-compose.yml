services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: imcipm
      POSTGRES_PASSWORD: imcipm
      POSTGRES_DB: imcipm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U imcipm"]
      interval: 5s
      timeout: 3s
      retries: 10

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrantdata:/qdrant/storage

  api:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
      qdrant:
        condition: service_started
    environment:
      DATABASE_URL: postgresql+psycopg://imcipm:imcipm@postgres:5432/imcipm
      QDRANT_URL: http://qdrant:6333
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_BASE_URL: ${DEEPSEEK_BASE_URL:-https://api.deepseek.com}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL:-deepseek-v4-pro}
      TENCENTCLOUD_SECRET_ID: ${TENCENTCLOUD_SECRET_ID:-}
      TENCENTCLOUD_SECRET_KEY: ${TENCENTCLOUD_SECRET_KEY:-}
      TENCENTCLOUD_REGION: ${TENCENTCLOUD_REGION:-ap-guangzhou}
      TENCENTSMS_SDK_APP_ID: ${TENCENTSMS_SDK_APP_ID:-}
      TENCENTSMS_SIGN_NAME: ${TENCENTSMS_SIGN_NAME:-}
      TENCENTSMS_TEMPLATE_ID: ${TENCENTSMS_TEMPLATE_ID:-}
      TENCENTSMS_TEMPLATE_PARAM_COUNT: ${TENCENTSMS_TEMPLATE_PARAM_COUNT:-1}
      EMBEDDING_PROVIDER: ${EMBEDDING_PROVIDER:-local}
      EMBEDDING_DIM: ${EMBEDDING_DIM:-256}
    ports:
      - "18005:18005"

volumes:
  pgdata:
  qdrantdata:
